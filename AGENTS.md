# AGENTS.md

Never edit files under `.canon/`.

Before making any edits to the project, check whether `.canon/` has uncommitted changes, including untracked files. If it does, make no changes anywhere in the project and ask a human to bring `.canon/` back to a clean git state.

If you think something is wrong with `.canon/check.yml`, ask a human to fix it.
